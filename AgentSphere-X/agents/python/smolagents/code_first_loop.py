def describe_code_first_loop() -> dict[str, object]:
    return {
        "framework": "smolagents",
        "mode": "code-first execution",
        "safety": "restricted MCP tool exposure + execution tracing",
    }

