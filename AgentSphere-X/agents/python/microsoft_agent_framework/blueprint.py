def get_framework_blueprint() -> dict[str, object]:
    return {
        "framework": "microsoft_agent_framework",
        "focus": "unified orchestration successor to AutoGen/Semantic Kernel",
        "contracts": ["tool adapters", "memory adapters", "policy middleware"],
    }

